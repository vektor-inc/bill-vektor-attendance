{
  "name": "bill-vektor-attendance",
  "version": "1.0.0",
  "scripts": {
    "dist": "npx gulp dist && cd dist && zip -r bill-vektor-attendance.zip bill-vektor-attendance"
  },
  "dependencies": {
    "@wordpress/env": "^4.8.0",
    "gulp": "^4.0.2",
    "run-sequence": "^2.2.1"
  },
  "devDependencies": {
    "npm-check-updates": "^13.1.1"
  }
}
